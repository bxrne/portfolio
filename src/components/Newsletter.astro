<link href="https://e.sensorpro.net/ScriptCache/SensorPro_RegForm.css" rel="stylesheet">

<form name="sensorProForm" id="sensorProForm" method="post" autocomplete="off" class="flex flex-col gap-4 w-full">
  <div class="flex items-center gap-4 w-full">
    <input
      type="email"
      name="spro_email"
      id="spro_email"
      placeholder="email@newsletter.com"
      class="flex-1 px-4 py-3 border-2 border-black text-black bg-transparent focus:outline-none focus:ring-0 text-lg"
      required
    >
    <button
      type="button"
      id="Spro-Btn-Signup-StyleNo"
      onclick="SensorProForms.initialize('RegForm','https://e.sensorpro.net/Run/Subscribe.aspx?fid=qzuo78qt%2FWe0h72YktFkNhkbYlylUGhWl1g%2BQov7mo2ZS5WO5UkSig%3D%3D_stzh','');SensorProForms.submitForm(false);"
      class="px-8 py-3 border-2 border-black text-black bg-transparent hover:bg-black/5 focus:outline-none focus:ring-0 text-lg font-medium whitespace-nowrap"
    >
      Sign Up
    </button>
  </div>
  <div class="flex items-center gap-2 text-sm">
    <input
      type="checkbox"
      id="spro_consent"
      class="custom-checkbox"
      required
    >
    <label for="spro_consent" class="text-black">I want emails!</label>
  </div>
    <div id="sensorProMsg" style="display:none;"></div>
</form>

<script is:inline>
// @ts-ignore
if (!window.SensorProForms) {
  const script = document.createElement('script');
  script.src = 'https://e.sensorpro.net/ScriptCache/SensorPro_RegForm.js';
  script.onload = () => {
    console.log('SensorPro script loaded');
  };
  script.onerror = () => {
    console.error('Failed to load SensorPro script');
  };
  document.head.appendChild(script);
}
</script>

<style>
  form {
    position: relative;
  }
  form::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgb(251, 241, 199);
    transform: translate(2px, 2px);
    z-index: -1;
  }

  /* Custom checkbox styling */
  .custom-checkbox {
    position: relative;
    width: 1rem;
    height: 1rem;
    border: 2px solid black;
    background: transparent;
    cursor: pointer;
    appearance: none;
    outline: none;
    transition: all 0.2s ease;
  }

  .custom-checkbox:hover {
    background: rgba(0, 0, 0, 0.05);
  }

  .custom-checkbox:checked {
    background: black;
  }

  .custom-checkbox:checked::after {
    content: '✓';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-size: 0.75rem;
    font-weight: bold;
  }

  .custom-checkbox:focus {
    box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.1);
  }
</style>
