---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---
<button id="back-to-top" class:list={["text-sm text-paper-muted hover:text-paper-fg transition-colors", className]}>
  ↑ Top
</button>
