---
type Props = {
  href: string;
  external?: boolean;
}

const { href, external, ...rest } = Astro.props;
---

<a
  href={href}
  target={external ? "_blank" : "_self"}
  style="color: var(--link)"
  class="underline underline-offset-2 decoration-from-font"
  {...rest}
>
  <slot/>
</a>